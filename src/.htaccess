<IfModule mod_autoindex.c>
	# Prevent directory browsing 
    Options -Indexes
</IfModule>

<IfModule mod_rewrite.c>
	# Apache Rewrite Rules
	Options +FollowSymLinks
	
	# Turn mod_rewrite on
	RewriteEngine On
	
	# Remove www from URL: www.example.com → example.com
	RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
	RewriteRule ^(.*)$ http://%1/$1 [R=301,L]
	
	# Force HTTPS: http:// → https://
	# RewriteCond %{HTTPS} !=on
	# RewriteRule ^/?(.*) https://%{SERVER_NAME}/$1 [R=301,L]

	# Remove .php from URL: /vdir/foo.php → /vdir/foo
    RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s([^.]+)\.php [NC]
    RewriteRule ^ %1 [R=301,L]
	
	# If request is a directory, serve index.php if it exists
	RewriteCond %{REQUEST_FILENAME} -d
	RewriteCond %{REQUEST_FILENAME}/index.php -f
	RewriteRule ^(.*)$ $1/index.php [L]

	# Internally map clean URLs to .php files
	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteCond %{REQUEST_FILENAME}\.php -f
	RewriteRule ^(.*)$ $1.php [L]
 
# End of Apache Rewrite Rules
</IfModule>
